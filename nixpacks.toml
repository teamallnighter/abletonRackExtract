# Nixpacks configuration for Flask backend only

[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"]

[phases.build]
cmds = [
  "cd backend && pip install -r requirements.txt"
]

[phases.start]
cmd = "cd backend && gunicorn app:app --bind 0.0.0.0:$PORT"
