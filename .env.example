# Railway MongoDB Environment Variables
# These are automatically provided when MongoDB is attached in Railway

# Primary MongoDB connection string
MONGO_URL=mongodb://username:password@host:port/database

# Alternative MongoDB URI (some providers use this)
MONGODB_URI=mongodb://username:password@host:port/database

# Individual MongoDB components (Railway provides these)
MONGOHOST=your-mongodb-host
MONGOPORT=27017
MONGOUSER=your-username
MONGOPASSWORD=your-password
MONGODB_DATABASE=ableton_rack_analyzer

# For local development
# MONGO_URL=mongodb://localhost:27017/ableton_rack_analyzer

# Authentication Configuration
JWT_SECRET_KEY=your-secret-key-here  # Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_LIFETIME_DAYS=7
