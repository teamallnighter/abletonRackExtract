# Railway Environment Variables Template
# Copy this to Railway's environment variables section

# JWT Secret Key - Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your_jwt_secret_key_here

# MongoDB URL - Automatically provided by Railway when MongoDB is attached
# MONGO_URL=mongodb://...

# Allowed Origins for CORS (comma-separated)
# For production, set this to your actual domain
ALLOWED_ORIGINS=https://yourdomain.railway.app,https://yourdomain.com

# Optional: Set to production for enhanced security
FLASK_ENV=production

# Optional: Debug mode (set to False in production)
FLASK_DEBUG=False

# OpenAI Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Pinecone Configuration
# Sign up at https://www.pinecone.io/ and create an index
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_ENVIRONMENT=us-east-1-aws
PINECONE_INDEX_NAME=ableton-racks
